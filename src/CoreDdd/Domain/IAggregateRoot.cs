namespace CoreDdd.Domain
{
    /// <summary>
    /// Marks an entity (= a class derived from <see cref="Entity"/> or <see cref="Entity{TId}"/>) as an aggregate root domain entity.
    /// </summary>
    public interface IAggregateRoot
    {
    }
}