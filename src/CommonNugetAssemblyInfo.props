<Project>

  <PropertyGroup>
    <AssemblyVersion>3.0.17.0</AssemblyVersion>
    <FileVersion>3.0.17.0</FileVersion>
    <Version>3.0.17</Version>
    <Authors>Martin Havlišta</Authors>
    <Company></Company>
    <Product />
    <PackageProjectUrl>https://github.com/xhafan/coreddd</PackageProjectUrl>
    <PackageLicenseUrl>https://raw.githubusercontent.com/xhafan/coreddd/master/LICENSE</PackageLicenseUrl>
    <PackageReleaseNotes>NhibernateUnitOfWork.Rollback() handles gracefully a situation when Session was disposed. Can happen when unit of work is committed, but transaction scope commit fails, and the error handling tries to rollback.</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SourceLink.Create.CommandLine" Version="2.8.1" PrivateAssets="All" /> 
  </ItemGroup>

</Project>