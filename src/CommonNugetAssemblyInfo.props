<Project>

  <PropertyGroup>
    <AssemblyVersion>3.0.21.0</AssemblyVersion>
    <FileVersion>3.0.21.0</FileVersion>
    <Version>3.0.21</Version>
    <Authors>Martin Havlišta</Authors>
    <Company></Company>
    <Product />
    <PackageProjectUrl>https://github.com/xhafan/coreddd</PackageProjectUrl>
    <PackageLicenseUrl>https://raw.githubusercontent.com/xhafan/coreddd/master/LICENSE</PackageLicenseUrl>
    <PackageReleaseNotes>Fixed HasManyForDomainConvention - when a child entity does not reference the parent entity, the child entity collection should not be marked as Inverse</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SourceLink.Create.CommandLine" Version="2.8.1" PrivateAssets="All" /> 
  </ItemGroup>

</Project>