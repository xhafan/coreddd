<Project>

  <PropertyGroup>
    <Version>3.2.3</Version>
    <PackageReleaseNotes>Fixed an issue:  
#3 Dependency injection does not release NhibernateConfigurator  

Fixed .NET Core dependency injection registration example: services.AddCoreDddNhibernate(serviceProvider => new NhibernateConfigurator(connectionString: "a connection string"));</PackageReleaseNotes>
  </PropertyGroup>

</Project>