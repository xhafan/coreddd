<Project>

  <PropertyGroup>
    <Version>3.2.3</Version>
    <PackageReleaseNotes>New feature:  
#4 Support NHibernate component automapping - added support for NHibernate component automapping ( override BaseNhibernateConfigurator.GetComponentTypes() ); made AutomappingConfiguration public so it can be derived from and further customized ( override BaseNhibernateConfigurator.GetAutomappingConfiguration() )</PackageReleaseNotes>
  </PropertyGroup>

</Project>